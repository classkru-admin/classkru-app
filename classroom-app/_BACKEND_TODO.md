# 🔧 งานหลังบ้านที่รอทำ (Backend TODO)

> Claude = ผู้ช่วยจัดการระบบหลังบ้าน (deploy/git/ไฟล์/โครงสร้าง)
> ไม่แก้ดีไซน์/UI เว้นแต่ผู้ใช้สั่ง

---

## ⏳ รอทำ (เมื่อผู้ใช้แก้ดีไซน์เสร็จ แล้วสั่ง)

### 1. Nav Component กลาง — แก้ทีเดียวจบทั้งระบบ
**ปัญหา:** เมนู (sidebar + bottom nav) ถูก hardcode ซ้ำใน 8 หน้า → แก้ทีต้องไล่ทุกหน้า เสี่ยงตกหล่น

**แผน:**
- สร้าง `ck-nav.js` ตัวเดียว เก็บโครงสร้างเมนูทั้งหมด (desktop sidebar + mobile bottom nav)
- ทุกหน้าดึงเมนูจาก ck-nav.js แทนเขียน HTML เอง
- เริ่ม **ขอบเขตเมนูหลักก่อน** (หน้าหลัก/ตารางสอน/เช็คชื่อ/สถิติ/ตั้งค่า) — ยังไม่รวม logo/topbar
- หลังทำเสร็จ: เปลี่ยนเมนู = แก้ที่ ck-nav.js ที่เดียว

**ขั้นตอนทำ (ทีละสเตป ทดสอบในคอมก่อน ยังไม่ push):**
1. สร้าง ck-nav.js รวมเมนูปัจจุบัน + logic active state
2. ทดลองกับ report.html ก่อน 1 หน้า → เปิดในคอมเทียบว่าเหมือนเดิมเป๊ะ
3. ผ่าน → ทยอยทำหน้าที่เหลือ (มี sidebar 8 หน้า: index, attendance, report, room-detail, schedule, settings, setup, subjects)
4. เก็บ CSS เดิมไว้ทั้งหมด แตะแค่ "ที่มา" ของ HTML เมนู ไม่แตะดีไซน์/สี/ขนาด

**รับประกัน:** หน้าตาเหมือนเดิมทุกพิกเซล, ทดสอบในคอมทุกขั้น, เก็บโค้ดเดิมย้อนได้

---

## ✅ เสร็จแล้ว (รอ push ลงเว็บทีเดียวตอนแก้ดีไซน์เสร็จ)

- จัดระเบียบไฟล์: ย้าย 10 ไฟล์เก่าเข้า `_archive/` (overview, dashboard, classroom_dashboard, attendance_system, attendance_swipe, assignments, scores, student, landing, seed)
- เพิ่ม `.vercelignore` กัน _archive ขึ้นเว็บ
- ตรวจแล้วไม่มี link เสีย แอปหลักเหลือ 10 หน้าสะอาด

---

## 📋 หลักการดูแล link/ข้อมูล (ทำให้เป็นระเบียบ)

เวลาแก้อะไรที่ "มีเหมือนกันหลายหน้า" (เมนู, หัวข้อ, รูปแบบข้อความ):
1. **เช็คก่อนว่าซ้ำกี่ไฟล์** — `grep -l "ข้อความ" *.html`
2. **ถ้าซ้ำหลายไฟล์ = ควรทำเป็น component กลาง** ไม่ไล่แก้ทีละหน้า
3. **แก้แล้วตรวจ link เสมอ** — ไม่มีหน้าไหนชี้ไฟล์ที่ย้าย/ลบ
4. **ทดสอบในคอมก่อน push**

---

_อัปเดต: 14 มิ.ย. 2569_
